# Netlify build configuration
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so React Router routes (e.g. /schedule) work on refresh
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
